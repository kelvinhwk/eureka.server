server:
  port: 80

spring:
  application:
    name: front-service

eureka:
  instance:
    host: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost2:8762/eureka/