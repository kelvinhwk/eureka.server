server:
  port: 8762

eureka:
  instance:
    hostname: localhost2
  client:
    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
      defaultZone: http://localhost:8761/eureka/

